numpy>=1.24.0
opencv-contrib-python==4.10.0.84
httpx==0.27.0
psutil==5.9.8
python-dotenv==1.0.1
fastapi==0.115.0
uvicorn==0.32.0
picamera2==0.3.17; platform_machine == 'aarch64'
gpiozero==2.0.1; platform_machine == 'aarch64'
pigpio==1.78; platform_machine == 'aarch64'

# PyTorch removed for Raspberry Pi compatibility
# MiniFASNet anti-spoofing is now optional and requires manual PyTorch installation
# For x86_64 systems that need MiniFASNet, install separately:
# pip install torch torchvision --extra-index-url https://download.pytorch.org/whl/cpu

onnxruntime>=1.15.0
